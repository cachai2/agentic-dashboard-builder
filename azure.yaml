name: ignite-autodash
services:
  cpu-app:
    project: .
    language: python
    host: containerapp
    docker:
      path: ./app/Dockerfile.cpu
  gpu-planner:
    project: .
    language: python
    host: containerapp
    docker:
      path: ./app/ollama_service/Dockerfile
infra:
  provider: bicep
  path: infra
